[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rubik-solve-core"
version = "0.1.0"
description = "Reference cube engine for Rubik solve video analysis"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["cubesolve_core*"]

